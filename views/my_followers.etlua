<% content_for('title', locale.get('follower_users')) %>
<h1 class="mb-3"><%= locale.get('follower_users') %></h1>

<%
render(
    'views.grid',
    {
        class = 'users',
        item_type = 'profile',
        items_per_row = 12,
        with_search = false,
        show_if_empty = true,
    }
)
%>
